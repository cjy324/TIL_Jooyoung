-CS 공부

# Spring Framework

---

- 자바로 **동적 웹어플리케이션을 개발하기 위해 필요한 필수적인 요소들을 갖추어둔 개발 틀**
- **개발자는 이 틀에 맞추어 코드를 작성하면 된다.**

# Spring Boot

---

- 기존 Spring에 여러가지 초기 **환경세팅들이 이미 갖추어진 프레임워크**
- 스프링과의 큰 차이는 **톰캣서버가 내장되어 있다는 점**이다.

# Annotation

---

- **스프링에서 이 클래스의 용도를 알려주는 이름표 같은 역할을 하는 개념**
- 스프링에는 빈이라는 객체가 존재한다.
- 빈은 스프링에서 사용하는 어플리케이션 객체이다.
- **스프링에서는 어노테이션을 활용해서 빈을 등록해 주어야하고 이를 통해 스프링이 그 객체를 인식할 수 있도록 해주어야 한다.**

## @Autowired

- 자동으로 특정 변수에 매칭해준다.

## @ResponseBody

- JSON형태로 응답할 수 있게 해준다.

# Mybitis

---

- **자바에서 관계형데이터베이스(DBMS) 관리를 좀 더 쉽게 해주는 프레임워크**이다.
- **한 두줄의 자바 코드로 DB 연동**을 처리
- SQL 명령어를 **자바 코드에서 분리하여 XML 파일에 따로 관리**

# Process vs Thread

---

## Process

- **단순히 실행 중인 프로그램(program)**
- **모든 프로세스는 1개 이상의 쓰레드로 구성**
- 두 개 이상의 스레드를 가지는 프로세스를 멀티스레드 프로세스(multi-threaded process)

## Thread

- **프로세스(process) 내에서 실제로 작업을 수행하는 주체**
